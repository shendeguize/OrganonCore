# Actual bounded skill responses

These are actual applications by the independent reviewer of the newly read Core methods and outer forwarding instructions. They are not separate blind agents reviewing this reviewer's own newly written test encoding. No recursive reviewer was commissioned. The lack of fresh blind review remains visible; migration behavior can be checked without certifying a philosophical theorem.

## Core and outer proof entrypoints — ordinary text

Input has two exact lines: craft.1 “A workshop values preserving an existing craft.” and craft.2 “This commitment does not require every implementation to use its oldest tool.” The first is a contextual value claim; the second limits the required consequences of that commitment and is not simply a report that some tool is never used. Both depend on the same workshop/commitment context. Grounds supports assessing the stated value's reasons/limits, not proving it from tool output; implementation openness does not grant older or newer tools unconditional priority.

Selected baseline is the explicitly supplied `Selected.md`, philosophy_version 0.7.2, retained as a downstream test baseline rather than replaced with Core. Core continues to govern the method. The input is a review object, not adopted philosophy. Both source lines are frozen with exact local spans and hashes.

No source-fidelity judgment is accepted for the test interface. `Workshop.Commitment` stores an arbitrary proposition called `valuesPreservingCraft`; the field name alone does not encode or establish what the workshop values. It is only a proposed interface, not proof of satisfaction or philosophical content. The second clause remains unmodeled. Whole-source result is incomplete. The first no-declaration attempt is preserved and was mechanically rejected as `Missing declaration inventory`; the next version adds a definition solely to exercise the mechanical mapping/build/audit path. This later implementation detail does not retroactively change the original preregistered acceptance criterion or turn the source into a proved statement.

Actual Core and outer checker invocations separately built and audited the same definition with no axiom dependencies, exit 0, semantic_status not_evaluated. This supports path/CLI parity and ordinary-text inventory usability, not a formalization achievement.

## Core and outer natural-language entrypoints — Lean-only

Input `Identity.lean`, line 1: `theorem retained (P : Prop) (h : P) : P := h`.

中文实际输出：任取命题 `P`，如果已经给出 `P` 的证明 `h`，则返回同一个 `h` 作为结论 `P` 的证明。`P : Prop` 声明任意命题；`h : P` 是显式前提；`: P` 是待返回的结论；`:= h` 直接使用已有证明。该声明没有独立建立 `P`，也没有证明任何未提供的自然语言主张。第二行 `#print axioms retained` 请求依赖审计，实际输出为 `retained` 不依赖任何公理。Lean 4.33.1 实际检查 exit 0。来源保真未评估，因为没有提供原文；代码解释不需要选择哲学基线。

Core direct invocation and outer forwarding preserve Chinese and --explain-lines scope: both yield this same single nonblank theorem-line explanation plus audit-line explanation. No installed translation CLI was invented. This is a standalone explanation, not a new blind semantic review of the maintained corpus.

## Explicit-baseline variation

Actual outer resolver returns Selected.md selectedPath/realPath/referenceDirectory and exact metadata 0.7.2. The direct Core method receives and retains this explicit choice. A nonexistent explicit missing.md returns exit 1 and “Correct the explicit path; no Core fallback.” The dependent philosophical operation remains incomplete; the source inventory above can still be retained. No baseline file was silently initialized or substituted.

Limits: this test does not certify all current bilingual manuscripts or every runtime branch. The independent source-first initial judgments preceded author-candidate disclosure; this small new encoding did not receive another independent source/Lean-blind pair, so it is not labeled semantically reviewed.
